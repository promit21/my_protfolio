/* eslint-disable react/prop-types */

const SectionTitle = ({ heading }) => {
  return (
    <div className="text-center">
      <h1 className="text-6xl font-bold py-4 text-white relative">{heading}</h1>
    </div>
  );
};

export default SectionTitle;
